# "mock" usa os repositórios em memória (src/repositories/mock). "api" fala com o backend real.
NEXT_PUBLIC_DATA_SOURCE=mock

# Base URL do backend NestJS (sem o prefixo /api/v1 — ele é adicionado automaticamente).
NEXT_PUBLIC_API_URL=http://localhost:8000
